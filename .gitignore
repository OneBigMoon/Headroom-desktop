node_modules
coverage
dist
target
.DS_Store
*.log
.env.local
.env
src-tauri/target
src-tauri/gen
announcement-recordings
headroom_memory.db

# Local git worktrees (isolated feature branches)
.worktrees/

# Local app bundle + sandboxed HOME used for manual desktop smoke tests. Holds
# machine-local state (client setup, router state, downloaded runtimes), never
# shared.
.local-test/

# Personal learn output, never shared
CLAUDE.local.md
